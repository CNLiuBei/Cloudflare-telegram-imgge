{
  "name": "telegram-image-bed",
  "version": "1.0.0",
  "description": "Cloudflare Workers + Telegram 无限空间图床",
  "main": "src/index.js",
  "scripts": {
    "build": "node src/build-worker.js && mkdir -p public && cp src/index.html public/",
    "dev": "npm run build && wrangler dev",
    "predeploy": "npm run build",
    "deploy": "wrangler deploy",
    "setup": "echo '请按照 SETUP.md 中的说明进行配置'"
  },
  "keywords": ["cloudflare", "telegram", "image-hosting"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
